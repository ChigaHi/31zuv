name: cli
description: A sample command-line application.
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  http: ^1.3.0
  command_runner:
    path: ../command_runner
  wikipedia:
    path: ../../wikipedia
  logging: ^1.2.0   # добавляем